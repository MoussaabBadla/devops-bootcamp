## health-check.conf
# Lightweight health endpoint for external load-balancers or orchestration.
location = /health {
    access_log off;
    default_type text/plain;
    add_header Content-Type text/plain;
    return 200 "ok";
}

# Include this inside a server block to expose /health
